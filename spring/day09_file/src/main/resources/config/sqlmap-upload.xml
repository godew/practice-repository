<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
	"-//mybatis.org//DTD Mapper 3.0//EN"
	"https://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.wedog.model.UploadDAO"> <!-- interface일 경우 패키지명 써줘야함 --> 
	<insert id="insertFile" parameterType="upload">
		insert into upload values(#{name}, ${age}, #{fileName})
	</insert>
</mapper>
